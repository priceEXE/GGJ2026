# 核心系统 API 调用指南

为了简化开发，本项目对**动画系统**和**粒子特效系统**进行了接口封装。所有常用的控制方法都可以通过以下两个单例/组件的接口直接调用。

---

## 1. 动画与角色装饰控制



**核心文件**: `Assets/Scripts/Gameplay/AnimeAndCostume/CharacterAnimeController.cs`
**接口**: `ICharacterAnimeController`

使用该接口可以控制角色的 Spine 动画、槽位显隐（装饰开关）以及闪烁效果。

**相关预制体**：

ICharacterAnimeController 在Player-P1、2、3、4根gameobj下

### 常用方法：
- `PlayAnimation(SpineAnimations anim, bool loop)`: 播放指定动画。
- `ShowSlot(SpineSlots slot)`: 显示某个装饰或部位。
- `HideSlot(SpineSlots slot)`: 隐藏某个装饰或部位（支持非 Spine 槽位如 Cannon/Gun）。
- `ToggleSlot(SpineSlots slot)`: 显示/隐藏某个装饰或部位（本来显示就改成隐藏，本来隐藏就改成显示）。
- `Flash(float interval, int count)`: **触发受击红闪或黑白闪效果**。
    - **含义**: 该方法用于通过二值化效果给玩家提供强烈的视觉反馈。常用于角色受到伤害、弹刀或进入无敌状态等瞬时反馈。底层通过切换材质的二值化参数实现。
    - **参数**: `interval` (每次闪烁的时长，秒), `count` (闪烁次数)。
    - **范例**:
    ```csharp
    当角色受到伤害时，触发 3 次黑白交替闪烁，每次间隔 0.1 秒
    characterController.Flash(0.1f, 3);
    ```

> **提示**: 可用的动画名和槽位名请查阅 `Assets/Scripts/Gameplay/AnimeAndCostume/SpineEnums.cs` 中的 `SpineAnimations` 和 `SpineSlots` 枚举。

---

## 2. 重要枚举定义
**定义文件**: `Assets/Scripts/Gameplay/AnimeAndCostume/SpineEnums.cs`

所有的动画 ID 和 槽位 ID 都定义在这里：
- **SpineSlots**: 包含 body, lefthand, face1, cake, Cannon, Gun 等。
- **SpineAnimations**: 包含 idle, run, jump, attack, dash 等。

---





## 3. 粒子特效系统

**注意**: 使用场景单例调用，预制体名为——中心化的粒子发射器
**核心文件**: `Assets/Script/Practice/ParticleManager.cs`
**接口**: `IParticleManager`

### 简易接口：

- **用于跳跃和落地**：`SpawnDustRing(Vector3 position, Vector2 normal, int count)`: 产生地面烟尘圈。只需传位置、法向和数量（默认推荐 5）。
- **用于攻击击中**：`SpawnCreamJet(Vector3 position, Vector2 direction, float force)`: 产生奶油喷射。只需传位置、方向和力度（默认推荐 30）。
- **用于冲刺开始**：`SpawnDustJet(Vector3 position, Vector2 direction)`: 产生灰土烟雾喷射。全预设参数，只需位置和方向。
